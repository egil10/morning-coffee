__pycache__/
*.pyc
.venv/
venv/
.DS_Store
# transient digest written by the Claude Code Action at repo root (leading slash
# so committed per-edition docs/newsletters/*/data.json are NOT ignored)
/data.json
